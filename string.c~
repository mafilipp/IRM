#include <stdio.h>
#include <stdint.h>

uint32_t bit_merge(uint16_t hn2, uint16_t hn1)


void main()
{
uint16_t hn1 = 0x1234;
uint16_t hn2 = 0xabcd;
uint32_t result;
result = bit_merge(hn1,hn2);
printf("\nmerging 0x%x and 0x%x results in 0x%x\n",hn1, hn2, result);
}
